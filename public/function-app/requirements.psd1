# Managed dependencies — installed automatically by Azure Functions on cold start.
# Pinned to major versions to avoid breaking changes.
@{
    'Az.Accounts'      = '3.*'
    'Az.Compute'       = '8.*'
    'Az.ResourceGraph' = '1.*'
}
